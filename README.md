# Simple Fullstack Auth System (React + Go + PostgreSQL)

โปรเจกต์ตัวอย่างระบบสมัครสมาชิกและเข้าสู่ระบบ (Register/Login) แบบ Fullstack ที่เน้นความปลอดภัยและพร้อมใช้งานผ่าน Docker

## 🚀 ฟีเจอร์หลัก
- **Frontend:** React + Vite + Tailwind CSS (Modern UI)
- **Backend:** Go (Gin Framework)
- **Database:** PostgreSQL
- **Security:** 
  - เก็บ JWT ใน **HttpOnly Cookie** (ป้องกัน XSS)
  - Hash รหัสผ่านด้วย **bcrypt**
  - รองรับ **CORS** แบบ Dynamic (Credentials supported)
  - จัดการ Secrets ผ่าน **Environment Variables**
- **Infrastructure:** พร้อมรันด้วย **Docker Compose**

## 🛠 โครงสร้างโปรเจกต์
- `/frontend`: React application
- `/backend`: Go API service
- `docker-compose.yml`: สำหรับรันทุกอย่างพร้อมกัน (Frontend, Backend, DB)
- `.env`: ไฟล์เก็บความลับ (เช่น JWT Secret)

## 🏃 วิธีเริ่มใช้งาน

### 1. ติดตั้งสิ่งที่จำเป็น
- [Docker](https://www.docker.com/products/docker-desktop/) และ Docker Compose

### 2. ตั้งค่า Environment Variables
โปรเจกต์ใช้ไฟล์ `.env` สำหรับจัดการความลับ:
```bash
cp .env.example .env
```
*(หมายเหตุ: ผมได้ทำการสร้างและสุ่ม JWT_SECRET ให้เรียบร้อยแล้วในขั้นตอนก่อนหน้า)*

### 3. รันระบบ
เปิด Terminal ในโฟลเดอร์โปรเจกต์แล้วรัน:
```bash
docker-compose up --build
```

### 4. เข้าใช้งาน
- **Frontend:** [http://localhost](http://localhost)
- **Backend API:** [http://localhost:8080](http://localhost:8080)
- **PostgreSQL:** `localhost:5432` (User/Pass: `postgres`/`postgres`)

## 🔑 API Endpoints
- `POST /register`: สมัครสมาชิก (`username`, `password`)
- `POST /login`: เข้าสู่ระบบ (จะได้รับ HttpOnly Cookie)
- `POST /logout`: ออกจากระบบ (ล้าง Cookie)

## 📝 หมายเหตุ
- ข้อมูลในฐานข้อมูลจะถูกเก็บไว้ถาวรผ่าน Docker Volume (`postgres_data`)
- **ความปลอดภัย:** ห้ามนำไฟล์ `.env` ขึ้น Git Repository (ควรเพิ่มใน `.gitignore`)